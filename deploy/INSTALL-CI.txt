# install java
apt-get install sun-java6-jdk


# install hudson
wget -O /tmp/key http://hudson-ci.org/debian/hudson-ci.org.key
sudo apt-key add /tmp/key
wget -O /tmp/hudson.deb http://hudson-ci.org/latest/debian/hudson.deb
dpkg --install /tmp/hudson.deb

	# dependance de hudson...
	apt-get install deamon

# g++
apt-get install "g++"

# make
apt-get install make

# apache2
apt-get install apache2
apt-get install libapache2-mod-fastcgi
apt-get install libapache2-mod-fcgid

# doxygen
apt-get install doxygen
apt-get install texlive-extra-utils 	# dependance



# WMSServer
apt-get install zlib1g-dev
apt-get install libcppunit-dev
apt-get install libjpeg62-dev
apt-get install libfcgi-dev
# install proj4
install manuelle version 4.7.0 => /usr/local/lib/

# montage exanet
adduser --help
adduser --uid 501 --gid 501 geopo
	# modif /etc/fstab
	172.20.1.61:/exavol/geoportail /mnt/geoportail nfs tcp,rw,addr=172.20.1.61 0 0
mkdir /mnt/geoportail
